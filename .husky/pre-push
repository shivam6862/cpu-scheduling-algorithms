#!/bin/bash

echo "Running npm run build to ensure the project builds successfully..."
cd web
if npm run build; then
  echo "Build successful. Proceeding with commit..."
else
  echo "Build failed. Aborting commit..."
  exit 1
fi
